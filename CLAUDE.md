# 이 저장소에서 일할 때 지킬 것

이 프로젝트에서 실제로 저지른 실수에서 나온 규칙이다. 사례는 전부 실화다.

---

## 1. "없다" 고 단정하기 전에 정의를 바꿔봐라

**두 번 틀렸다.**

| 내가 한 말 | 근거 | 실제 |
|---|---|---|
| "검증된 매도 지표가 없다" | 규칙군 7개만 봄 | 규칙군을 넓히니 RSI 이탈이 나옴 |
| "아크 매도는 무효다" | **주간 매도 신호**만 봄 | **누적 감소**로 바꾸니 최고 규칙이 됨 |
| "아크 매도는 무효다" (2번째) | **하위 10% 분위수**만 봄 | **절대 규모(-7%)** 로 보니 3개월 하락률 100%, p=0.013 |

세 번째 사례가 결정적이다. 같은 "아크 매도" 를 세 가지로 정의했는데 결과가
전부 달랐다. 그런데도 나는 첫 정의 하나로 "무효" 라고 두 번 말했다.

같은 데이터인데 **묻는 방식**이 달랐다.

```
"이번 주에 팔았나"     -> 0.91배  (잡음. 아크는 매주 사고판다)
"진입 이후 -20% 줄었나" -> 2714   (실제로 발을 뺐다는 신호)
```

**부정 결과를 보고할 때는 반드시 "어떤 정의로 시험했는지" 를 함께 적어라.**
"X 는 효과 없다" 가 아니라 "X 를 이렇게 정의해 시험했더니 효과 없었다" 다.
그리고 다른 정의를 최소 두 개는 더 시험한 뒤에 말해라.

## 2. 성적이 아니라 원칙으로 골라라

'6개월 고정 보유' 는 성적이 났지만 사용자가 "원칙이 없다" 고 지적했다.
진입 규칙의 **거울상**을 찾으라는 요구였고, 그렇게 만드니 성적도 18.5% 올랐다.

```
진입:  아크가 사고  + 낙폭 깊을 때
청산:  아크가 줄이고 + 낙폭 회복
```

**대칭·인과·해석 가능성이 성적보다 먼저다.** 원칙이 서면 성적은 대개 따라온다.
성적만 좋고 설명이 안 되는 규칙은 과적합일 확률이 높다.

## 3. 최고값이 아니라 고원(plateau)을 봐라

파라미터를 훑고 **구간 전체의 중앙값과 최솟값**으로 판단한다. 최댓값은 보고하지 마라.

```
MACD 데드크로스   12/26/9 에서 4.28배  <- 인접 8/17/9 은 0.87배. 우연이다.
RSI 이탈          66~74 연속 6.68~7.10배  <- 고원이다.
아크+낙폭 청산     격자 35칸 전부 기준선 초과  <- 확실한 고원.
```

**채택값은 격자의 중앙에서 고른다. 최고값을 고르는 순간 과적합이다.**

## 4. 틀을 바꾸면 이전 결론을 전부 다시 골라라

자본 모델을 '단일 포지션 병합' -> '신호마다 독립 100' 으로 바꿨는데
매도 규칙을 다시 고르지 않았다. 순위가 뒤집혀 있었다.

```
단일 포지션 틀:  RSI 70 이탈 7.02배 > 6개월 고정 3.80배
독립 포지션 틀:  6개월 고정 2290   > RSI 70 이탈 2093   (역전)
```

**틀(표본 정의·자본 모델·비교 기준)을 건드렸으면 그 위에서 고른 모든 선택을
다시 검증하고, 무엇이 바뀌었는지 사용자에게 먼저 말해라.**

## 5. 겹치는 관측을 독립으로 세지 마라

낙폭 -60% 인 날이 28일이어도 그것은 **국면 1개**다.
일 단위로 세면 승률 100%, p=0.000 이 나오지만 통계가 아니라 중복이다.

**30거래일 이내는 한 사건으로 묶는다.** 무작위화 검정의 귀무분포도
같은 군집 구조를 가져야 한다.

## 6. 비교 기준은 같은 기간에서 뽑아라

테슬라는 2010년 이후 200배 올랐다. 15년 전체를 기준으로 2021년 이후 신호와
비교하면 **어떤 신호든 전 구간 음수**로 나온다(실측 -13% ~ -39%).

## 7. 알고리즘을 바꾸면 소급 신호를 먼저 확인해라

문턱을 절대 주식수 -> 비율로 바꾸자 **지난주가 소급해서 신호**가 됐다.
사용자는 그걸 나중에 표에서 발견했다.

**규칙을 바꾼 직후 "그래서 지금 신호가 있나" 를 내가 먼저 말해라.**

## 8. 구조를 바꾸면 알림·화면까지 한 번에 점검해라

포지션 모델을 바꾸면서 워크플로와 사이트가 옛 필드를 참조해
**알림이 빈 값으로 나갈 상태**였다. 같은 종류로 세 번 겪었다.

- 출력 필드 변경 -> 워크플로 env, 사이트 렌더 함수
- pyyaml 은 중복 키를 허용한다(깃허브 파서는 거부) -> 검증기에 중복 키 검사
- 스텝 순서 -> 값을 만드는 스텝이 직접 출력해야 한다

**바꾼 뒤 실제로 워크플로를 돌려 확인한다. 코드만 보고 넘어가지 않는다.**

## 9. 숫자는 원본에서 다시 계산해 대조해라

`verify_table.py` 는 화면의 모든 셀을 야후 원본에서 **다시 구현해** 대조한다.
계산 함수를 재사용하면 같은 실수를 그대로 통과시킨다.

이중 반올림으로 0.1%p 어긋난 것도 이 검증기가 잡았다.

## 10. 승률 100% 를 보면 구조를 의심해라

완료 매매 승률 100% 는 **손실 포지션이 청산되지 않아 통계에서 빠지는** 구조였다.
"청산 조건이 이익 상태에서만 걸리는가" 를 항상 확인하고,
**열려 있는 포지션을 포함한 수치를 함께 제시해라.**

---

## 10-1. 기준은 성적으로 고르지 마라

감정을 배제하려고 알고리즘을 쓴다. **성적을 보고 기준을 바꾸면 그 순간
알고리즘이 아니라 사후 합리화가 된다.**

낙폭 기준 창을 훑었더니 사상 최고가(3147) 가 252일(2714) 보다 좋아 보였다.
그런데 189일~사상최고가 구간은 **신호 집합이 16개로 전부 같았다** —
창을 늘리면 낙폭이 깊게 계산돼 문턱이 느슨해질 뿐이었다.
'더 좋은 규칙' 이 아니라 '같은 규칙에 완화된 문턱' 이다.

**성적 차이를 보면 먼저 "이게 정말 다른 규칙인가, 같은 규칙의 다른 라벨인가"
를 확인해라.** 신호 집합이 같으면 그것은 발견이 아니다.

## 10-2. 채택값의 근거를 즉시 적어라

낙폭 문턱을 -30% 로 정해놓고 **왜 -30% 인지 문서에 적지 않았다.**
사용자가 "이유 말해봐" 라고 물었을 때 다시 계산해야 했고,
확인해보니 -25% 와 -30% 는 결과가 완전히 같았다(그 사이 신호가 0개).

근거가 있는 것은 값이 아니라 경계였다:
  -20% 보다 느슨하면 최악이 -3.5% -> -24.4%
  -35% 보다 조이면 신호가 16 -> 11개

**파라미터를 정하는 순간 "이 값인 이유" 와 "무차별 구간이 어디까지인지" 를
함께 적어라.** 나중에 물으면 사후 합리화가 되기 쉽다.
값이 아니라 **경계**에 근거가 있다면 그렇게 쓴다.

## 11. 틀렸으면 틀렸다고만 써라

**이유를 덧붙이면 그게 변명이 된다.**

실제로 이렇게 썼다가 지적받았다:

> ~~"제가 '아크 하나뿐'이라고 쓴 건, 지금은 낙폭이 이미 통과 상태라
> 남은 변수가 아크뿐이라는 뜻이었는데 문장을 부정확하게 썼습니다."~~

이 문장은 **속으로는 맞게 알고 있었는데 표현만 실수했다**는 어감을 준다.
실제로는 그냥 틀리게 쓴 것이고, 지적이 없었으면 그대로 남았을 것이다.

```
쓰지 말 것:  "~라는 뜻이었는데 문장이 부정확했습니다"
             "~로 이해될 수 있게 썼습니다"
             "정확히는 ~인데 줄여 쓰다 보니"

쓸 것:       "틀렸습니다. 신호는 두 조건입니다."
```

**순서: 틀렸다 → 맞는 내용 → (필요하면) 짧은 원인.**
원인 설명이 인정보다 길면 그것은 변명이다.

## 12. 사용자가 이상하다고 하면 대체로 맞다

이 프로젝트에서 사용자가 지적한 것들:

| 지적 | 결과 |
|---|---|
| "더 찾아봐" (매도 지표 없다고 했을 때) | RSI 이탈 규칙이 나옴 |
| "아크 매도도 넣어봐" | 누적 감소로 정의를 바꾸니 최고 규칙 |
| "6개월 고정은 원칙이 없다" | 대칭 규칙을 찾으니 성적도 18.5% 상승 |
| "7일 간격이 아닌데?" | 매수일만 표시해 생긴 오해 → 신호일 병기로 수정 |
| "16회인데 왜 10개뿐이야" | 국면 묶음 표기가 불친절했음 |
| "점수 높은데 확률 0%는 앞뒤가 안 맞잖아" | 설명이 빠져 있었고, 내 문장도 틀렸음 |
| "보유 여부 말고 데이터로 판정해야지" | 상태 파일 의존 버그를 잡음 |
| "신호는 두 개 보는 거 아니었어?" | 내가 "아크 하나뿐"이라고 틀리게 썼음 |

**전부 옳았다. 방어하지 말고 확인부터 해라.**

---

## 13. 상대 기준과 절대 기준을 둘 다 시험해라

**분위수(상대)로 무효였던 것이 절대 규모로는 강한 신호였다.**

```
아크 매도, 하위 10% 분위수  ->  0.91배. "무효"
아크 매도, 순매수율 ≤ -7%   ->  3개월 -27.3%, 하락률 100%, p=0.013
```

분위수는 **평범한 값도 하위 10% 에 들어간다.** 매주 조금씩 파는 종목이라면
"하위 10% 매도" 는 그냥 평범한 주다. 반면 절대 기준은 **사건의 크기**를 본다.

**어떤 지표든 두 방식으로 정의해보고, 다르면 왜 다른지 설명해라.**

- 상대: 분위수, z-score, 자기 이력 대비 백분위
- 절대: 고정 임계, 규모(주식 수·금액), 비율의 절대값

## 14. 시각화에서 튀는 것을 내가 먼저 설명해라

사용자가 차트에서 이상한 것을 발견해 물어본 것이 한 세션에 세 번이었다.

| 사용자가 본 것 | 실제 |
|---|---|
| 보유량 차트의 뾰족한 골 | 데이터 구멍 -97.6% (펀드 동시 제거) |
| 주간 순매수의 유독 큰 빨간 막대 | 대량매도 신호 — 검증조차 안 한 것 |
| 문턱선이 점처럼 보임 | 축 범위 대비 변동폭이 작아서 |

**차트를 만들었으면 내가 먼저 훑고, 눈에 띄는 이상점을 설명하거나 조사해라.**
사용자가 발견하기 전에 하는 것이 일이다.

## 15. 하나의 발견을 여러 각도로 끝까지 밀어라

대량매도 검증에서 내가 실제로 한 것:

```
1. 분위수로 시험  ->  무효
2. (여기서 멈췄다)
```

했어야 할 것:

```
1. 분위수로 시험            ->  무효
2. 절대 규모로 시험          ->  유효 (p=0.013)
3. 1% 단위로 훑어 고원 확인   ->  -4~-9% 전 구간 음수
4. 기간 분할                ->  전·후반 모두 하락률 100%
5. 청산 규칙에 넣어봄         ->  개선 안 됨 (하락 예측 ≠ 청산 신호)
6. 경보로만 표시하기로 결정
```

**"효과 있다/없다" 로 끝내지 말고, 있으면 어디에 쓸지까지 정해라.**
이번 것은 하락 예측이지 청산 신호가 아니어서 판정에 넣지 않고 경보로 분리했다.

## 16. 문서를 고칠 때는 append 를 쓰고 결과를 확인해라

CLAUDE.md 에 규칙을 추가하면서 '중복 절 제거' 로직을 넣었다가
새로 쓴 절까지 함께 잘라냈다. **두 번 반복했다.**

- 규칙 추가는 `>> ` append 로 한다. 문자열 슬라이싱으로 잘라내지 않는다.
- 고친 뒤 `grep -n "^## "` 로 절 목록을 확인한다.
- grep 결과만 보고 넘어가지 말고 의도한 절이 다 있는지 센다.

---

## 보고할 때

- 표본 수를 항상 같이 적는다. n=7 이면 n=7 이라고 쓴다.
- 시험한 조합 수를 밝힌다. 다중검정 보정 기준도 함께.
- **틀렸던 것을 숨기지 말고 정정 이력을 남긴다** (docs/DECISIONS.md).
- 사용자가 "더 찾아봐" 라고 하면 그것은 대체로 옳다.

---

## 17. 성격이 다른 신호는 합치지 말고 병렬로 둬라

대량매도를 청산 규칙에 AND 로 넣었을 때 개선이 없었다. 그래서 "무효" 라고 했는데,
**병렬로 두니 유효했다**(평가 2689 -> 2819, 최악 -9.2% -> -3.5%).

이유는 두 신호의 성격이 달랐기 때문이다:

| | 규칙 A | 규칙 B |
|---|---|---|
| 뜻 | 목표 달성, 이익 실현 | 전제가 깨짐, 탈출 |
| 발동 | **이익 상태에서만** | 손익 무관 |

A 는 "낙폭 회복" 이 조건이라 이익일 때만 걸린다. B 는 손실 중에도 걸린다.
**AND 로 묶으면 B 가 죽고, 하나의 규칙에 욱여넣으면 둘 다 약해진다.**

**"이 신호를 기존 규칙에 어떻게 넣을까" 를 묻기 전에
"이것이 기존 규칙과 같은 종류의 사건인가" 를 먼저 물어라.**
다른 종류면 별도 규칙으로 병렬 배치하고, 먼저 걸리는 쪽을 쓴다.

## 18. 평가액이 아니라 최악 사례를 봐라

낙폭 필터를 없애면 평가액이 더 높았다(3213 vs 2821). 그래도 채택하지 않았다.

**평가액은 표본을 다 더한 값이라 사건이 늘면 자연히 커진다.**
필터를 풀면 신호가 16 -> 20건이 되고, 그 4건이 평균 이하라도 총합은 올라간다.

봐야 할 것은 **최악 사례**다: -1.9% -> -23.2%.
필터의 존재 이유는 평균을 올리는 것이 아니라 **꼬리를 자르는 것**이다.

규칙을 비교할 때 표본 수가 다르면 평가액·누적 배수는 공정한 비교가 아니다.
평균·승률·최악을 함께 보고, 특히 **최악이 어디서 나빠지는지** 확인해라.

## 19. 같은 값을 재는 세 가지 방식을 구분해라

"아크가 얼마나 팔았나" 를 재는 방식이 셋인데 성질이 전부 다르다.

| 방식 | 수식 | 성질 |
|---|---|---|
| **수량 기준** | ΔS / S | 채택. 순수 매매만 반영 |
| 금액 기준 | (ΔS × P) / (S × P) | **수량과 동일** (P 가 약분됨, r=0.9966) |
| 평가액 기준 | Δ(S × P) / (S × P) | **틀림.** 주가 변동이 섞임 (r=0.502) |

평가액 기준을 쓰면 2026-07-24 처럼 **아크가 +5.29% 산 주가 -13.46% 매도로 잡힌다**
(주가가 -17.8% 빠져서). 매수 신호와 매도 신호가 같은 주에 뜬다.

**"무엇을 재는가" 를 수식으로 써보면 같은 것인지 다른 것인지 즉시 보인다.**
곱하고 나누는 항이 약분되면 새로운 지표가 아니다. 약분되지 않으면
그 항이 무엇을 섞어 넣는지 확인해라.

## 20. 규칙을 바꾸면 문서를 자동으로 대조해라

매도 규칙을 세 번 바꾸는 동안 문서 곳곳에 옛 값이 남았다.
사람이 눈으로 찾으면 놓친다. 실제로 `docs/HOW_IT_WORKS.md` 의 매도 절이
폐기된 규칙을 그대로 설명하고 있었다.

`check_docs.py` 가 코드 상수를 읽어 현재 사양 문서에 그 값이 있는지 대조한다.

**문서를 두 종류로 나눠라.**

| 종류 | 파일 | 옛 값이 남아도 되나 |
|---|---|---|
| 현재 사양 | README, ALGORITHM, HOW_IT_WORKS | **안 된다** — 자동 검사 |
| 정정 이력 | DECISIONS, FINDINGS, CLAUDE | **정상** — 그것이 기록의 목적 |

이력 문서에는 맨 앞에 "이것은 검증 이력이며 현재 규칙이 아니다" 를 명시하고
현재 규칙 요약을 함께 둔다. 그래야 나중에 읽는 사람이 헷갈리지 않는다.

## 21. 병렬로 붙이는 규칙은 '사건'이어야 한다. '상태'를 붙이면 국면 내내 발화한다

매도에서 배운 17번(성격이 다른 신호는 병렬로 둬라)을 매수에 그대로 옮겼다가 실패했다.
극성만 옮기고 **조건의 성질**을 옮기지 않은 것이 원인이다.

| | 조건 | 성질 | 결과 |
|---|---|---|---|
| 매도 규칙 B | 단일 주 순매수율 ≤ -7% | **사건** — 그 주에만 참 | 5건, 병렬이 통했다 |
| 매수 규칙 B(기각) | 낙폭 ≤ -45% AND 아크 상위 40% | **상태** — 국면 내내 참 | 12건, 2022-10~12 에 7주 연속 |

상위 40% 문턱의 중앙값은 **0.38%** 였다. '아크가 조금이라도 사면' 참이므로
낙폭이 깊은 국면이 석 달 이어지면 열두 주 내내 신호가 켜진다.

**병렬 항을 추가하기 전에 물어라: 이 조건은 며칠 동안 참인가?**
하루면 사건이다. 몇 달이면 상태이고, 그대로 붙이면 같은 국면을 여러 번 산다.
상태를 꼭 쓰려면 사건으로 바꿔라 — 진입 시점만, 또는 직전 발화 대비
N 만큼 더 나빠졌을 때만(사다리). 그냥 붙이지 마라.

## 22. 신호를 늘리는 규칙은 **평가액으로 고르면 반드시 통과한다**

매수 규칙 B 를 채택한 근거는 격자였다. 25칸 중 17칸이 기준선(2874)을
'넘었다'. 그런데 그 격자의 셀 값이 **평가액**이었다.

평가액 = 건수 x 100 x (1+평균). 신호를 늘리는 규칙은 건수가 늘어나므로
평균이 떨어져도 평가액은 올라간다. **통과할 수밖에 없는 시험이었다.**
셀을 포지션당 평균으로 바꾸니 20칸 중 **19칸이 A 단독보다 못했다.**

| | 건수 | 평균 | 중앙값 | 최악 | 투입 -> 평가 |
|---|---|---|---|---|---|
| A 단독 | 16 | **+76.4%** | **+78.0%** | -1.1% | 1600 -> 2822 |
| A \| B | 28 | +70.1% | +66.5% | -1.1% | 2800 -> 4764 |

B 가 손해를 낸 것도 아니다 — B 의 12건은 평균 +61.8%, 승률 100% 로
그 자체로는 괜찮다. 다만 A 보다 못한 자리에 1200 을 더 넣는다.

**건수가 달라지는 후보끼리는 총액을 비교하지 마라. 단위당으로 비교해라.**
18번(평가액이 아니라 최악 사례)의 짝이다. 최악 사례는 -1.1% 로 똑같았고
그마저 보유 중 포지션이라 매일 바뀐다. **최악만 봤으면 이것도 못 잡았다.
평균·중앙값·최악을 함께 봐라.**

## 23. 문턱은 파이프라인과 **같은 지점**에서 계산해라

규칙 B 를 재검증하다가 격자가 정반대로 나왔다(19칸 실패 -> 1칸 실패).
원인은 데이터도 규칙도 아니고 **분위수를 계산한 위치**였다.

```python
w = build(px)                                   # build 끝에서 dropna 로 앞 52주가 잘림
thr = w["netpct"].expanding(52).quantile(q)     # 틀렸다 — 잘린 뒤라 창이 다시 시작
```

`build` 는 자르기 **전** 296주에서 문턱을 낸다. 자른 뒤 243주에서 다시 내면
앞 52주가 NaN 이 되고 문턱 값 자체도 달라진다(상위 40% 중앙값 0.38% -> 0.47%).
신호 수가 28 -> 21 로 바뀌고 결론이 뒤집힌다.

**검증 스크립트를 짤 때 첫 줄은 '기존 규칙을 재현하는가' 여야 한다.**
규칙 A 를 그 스크립트로 계산해서 16건이 안 나오면, 그 스크립트로 낸
B 의 숫자도 전부 못 믿는다. 이 한 줄이 없어서 잘못된 격자를 문서에 쓸 뻔했다.

**규칙 선정용 백테스트는 라이브 실행 코드와 같은 함수를 호출해라.**
청산은 `position_tracker.replay()` 를 그대로 쓴다. 재구현하면 갈라진다.

**그리고 성적이 안 나오면 그냥 버려라.** 빈틈을 찾은 것(낙폭 -48~-54% 인데
신호 0건인 구간)은 진짜 발견이었지만, 발견했다고 반드시 규칙이 되는 건 아니다.
"찾았고, 원칙 있는 해법을 시험했고, 기준선을 못 넘어 버렸다" 는 완결된 답이다.
